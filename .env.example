# ── docker-compose variables ──────────────────────────────────────────────────

# URL the browser uses to reach the mock API (baked into frontend bundle at build time)
VITE_API_BASE_URL=http://localhost:4000

# Host ports for the two services (change if these ports are already in use)
FRONTEND_PORT=3000
MOCK_PORT=4000
